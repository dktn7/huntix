# Huntix — Cursor Rules
# Auto-loaded every session. Never delete or override these.
# MAINTAINER NOTE: Update the "Development Phase" section below when advancing phases.
#
# ── WHY THIS FILE IS STRUCTURED THIS WAY ────────────────────────────────────
# LLMs exhibit U-shaped attention: strongest at the start and end of context,
# weakest in the middle. Critical constraints (widget, phase, do-not list) are
# placed at the TOP of this file intentionally. The phase block is near the top
# so it is never lost-in-the-middle during long sessions.
# Source: context-degradation pattern (muratcankoylan/agent-skills-for-context-engineering)
# ─────────────────────────────────────────────────────────────────────────────

## Non-Negotiable Vibe Jam Rules
- The widget script MUST always be present in index.html:
  `<script async src="https://vibej.am/2026/widget.js"></script>`
- No loading screens. Game must be playable within seconds of opening URL.
- Free-to-play, no login, public URL, single domain.

## Development Phase (Current)
# ─── UPDATE THIS BLOCK WHEN ADVANCING PHASES ───────────────────────────────
Current Phase: 1 — Core Engine (Days 1–3)
Already built: GameLoop.js | Renderer.js | SceneManager.js | InputManager.js | main.js
Next to build: PlayerState.js → CombatController.js → Hitbox.js → ManaBar.js → EnemyAI.js → EnemySpawner.js
Do NOT add yet: character models, textures, audio, zone transitions, P2+ input
# ───────────────────────────────────────────────────────────────────────────

## Do Not
- Add npm packages or build tools
- Use perspective camera
- Add loading screens or splash delays
- Use eval() or runtime dynamic import()
- Commit large binaries to repo root — use assets/
- Build features not in the spec without flagging first
- Remove or modify the Vibe Jam widget script
- Implement P2–P4 input before Phase 3
- Build HUD or shop UI before Phase 5

## Project Identity
Huntix is a 2.5D browser beat 'em up / roguelite brawler built in Three.js r169 for Vibe Jam 2026.
Deadline: 1 May 2026 @ 13:37 UTC. 18-day build window.

## Tech Stack (Do Not Change)
- Three.js r169 via CDN importmap — NO npm, NO Vite, NO bundler, NO build step
- ES modules only (`type="module"`)
- Orthographic camera — never switch to perspective
- No dynamic `import()` at runtime
- Do not upgrade Three.js mid-jam

## Game Loop — Fixed Timestep (Critical)
- GameLoop.js uses a FIXED timestep accumulator: FIXED_DT = 1/60 = 0.01667s always
- The callback always receives exactly 0.01667s — never a variable dt from RAF
- MAX_DT cap is 1/20 (50ms) to prevent spiral of death after tab switch
- All gameplay code must be written for fixed dt, not variable dt
- Do NOT read performance.now() or RAF timestamps in gameplay code — use the dt passed in

## Real Input Bindings (from InputManager.js — do not invent new ones)
P1 Keyboard:
  Move: WASD + Arrow keys
  Light attack: J
  Heavy attack: K
  Dodge: Shift (left or right)
  Special: E
  Interact: F
  Pause: Escape
P1 Gamepad (index 0):
  Move: axes[0] (LX), axes[1] (LY) — deadzone 0.3
  Interact: button 0 | Dodge: button 1 | Light: button 2 | Heavy: button 3
  Special: button 5 | Pause: button 9
P2–P4 Gamepad: NOT YET IMPLEMENTED — must be built in Phase 3
P2 Keyboard: NOT YET IMPLEMENTED — must be built in Phase 3

## Performance Limits
- Max 20 enemies on screen — use THREE.InstancedMesh for grunts
- Max 500 particles — pool and reuse, never allocate in game loop
- No dynamic shadows in combat scenes
- No new THREE.Vector3() / Matrix4 / Color inside the animation loop
- Target 60fps on Intel Iris / integrated GPU
- All assets preloaded before first frame — no runtime fetches
- Total initial asset payload < 3MB

## Code Conventions
- One class per file
- `src/engine/` = core systems, `src/gameplay/` = game logic
- All movement uses `dt` — always 0.01667s (fixed timestep), never frame-count timers
- All input goes through InputManager.isDown(action) — never read raw keys directly
- State machines use named string constants — no magic strings inline
- No console.log in shipped code except behind `if (DEBUG)` flag
- Comment every public method with a one-line description

## Combat Feel Spec
- Light attack hitstop: freeze dt for 80ms
- Heavy attack hitstop: freeze dt for 150ms
- Dodge: 300ms total, 200ms i-frames, directional from moveVector
- Every hit must produce: spark particles + screenshake + stagger + SFX
- Enemy must show attack telegraph before every hit
- Only valid cancel: any attack → dodge

## Hunter Quick Reference
| Hunter  | Element       | HP  | Mana | Dodge           | Status |
|---------|---------------|-----|------|-----------------|--------|
| Dabik   | Shadow        | 80  | 120  | Blink           | Bleed  |
| Benzu   | Thunder/Earth | 160 | 70   | Shoulder Charge | Stun   |
| Sereisa | Lightning     | 100 | 100  | Electric Dash   | Slow   |
| Vesol   | Flame         | 90  | 130  | Flame Scatter   | Burn   |

## Phase Unlock Gates (do not build ahead of schedule)
| Phase | Days | Gate |
|---|---|---|
| 1 | 1–3  | Core engine + combat input |
| 2 | 4–6  | Enemy AI + hit detection + juice |
| 3 | 7–9  | Hunter models + 4P input + camera |
| 4 | 10–12 | Zones + boss phases + essence drops |
| 5 | 13–15 | Hub shop + XP + HUD |
| 6 | 16–18 | Audio + onboarding + deploy |

## Design Docs
Before implementing any system, read the relevant doc in docs/:
GDD.md | MVP-PLAN.md | HUNTERS.md | BOSSES.md | ENEMIES.md | ZONES.md
INPUT.md | COOP.md | WEAPONS.md | HUD.md | AUDIO.md | TECHSTACK.md
